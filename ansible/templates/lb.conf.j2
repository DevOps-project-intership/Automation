upstream flask_app {
{% for host in groups['server'] %}
    server {{ host }}:80;
{% endfor %}
}

server {
    listen 80;
    location / {
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://flask_app;
    }
}
